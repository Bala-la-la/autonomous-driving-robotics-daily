# 分类总结

本页是跨期观察，不替代每日原始报告。自动更新会在出现新的、可重复验证的研究路线或社区偏好时补充本页。

## 2026-08-15 新增判断

- **自动驾驶**：失败自进化与轨迹引导检索把稀有事故和运动语义分别变成可训练信号，驾驶 VLA 的数据闭环正从“再收集更多样本”转向“解释失败并按物理行为索引”。
- **机器人／具身智能**：世界模型开始承担动作前监控和几何一致性职责；任务进度 probe、接触前 abort 与 SE(3) 条件预测共同形成可观测、可中止的部署接口。
- **SLAM／长期自治**：本期无新的 SLAM 主线，但多无人机 HOCBF-QP 显示不确定性边界正在从感知估计延伸到群体执行安全，值得与长期地图风险联动跟踪。
- **GitHub Agent／Skills／基础设施／生产力**：榜单同时奖励设计／3D 成品和规格、RAG、图上下文等底层约束，社区偏好从模型能力转向可交付、可审计、可本地运行的完整工作流。

## 2026-08-12 新增判断

- **自动驾驶**：推理正被拆成场景规划关键因子、轨迹奖励搜索和路侧／车端双时域 latent；评价重点从“能否解释”转向能否降低碰撞、通信和端侧成本。
- **机器人／具身智能**：扩展瓶颈从参数量转向可复用接口与监督。统一策略 adapter 把 N×M 集成降为 O(N+M)，时间距离把廉价时间戳转成跨任务 value supervision。
- **SLAM／长期自治**：定位和规划开始显式建模分布偏移与物理结构；Wasserstein 协方差鲁棒化和能量—动量 latent 都把安全约束放进状态更新本身。
- **GitHub Agent／Skills／生产力**：开源栈进一步分成自改进 harness、并行 ADE、工作管理控制面、Skills 分发和图原生上下文；垂直视频系统显示领域工艺资产正成为竞争点。

## 2026-08-11 新增判断

- 驾驶 VLA 与腿式控制都开始显式学习“何时计算、何时切换”：规划 token 可按层早退，步态相位可按故障自适应，推理深度与控制时序正在成为闭环策略的一部分。
- 长期自治的空间记忆从对象标签扩展到跨楼层、跨会话的持久 3D 语义图；与此同时，表示交接和接触一致重定向把多模块系统的接口误差暴露为主要瓶颈。
- 触觉研究从连续力估计走向瞬态接触事件检测，可能成为机器人安全控制的高频旁路，但传感器耐久和材料泛化仍需真实部署证据。
- GitHub 社区继续奖励可运行的 Agent 基础设施：Skills、harness、代码图 RAG 与多 Agent 工作流并列升温，浏览器和媒体采集则把 Agent 推向真实数据边界。

## 2026-08-10 新增判断

- 世界动作模型的部署优化从固定裁剪推进到决策质量驱动的动态算力：未来预测可以只作为训练监督，中间 DiT 特征直接解码轨迹，并由质量评分选择早退深度；下一步关键是评分校准与长尾场景不过早退出。
- 跨本体 WAM 的主要障碍被进一步定位为“外观迁移不等于动力学迁移”：现有模型更依赖视觉相似性，数值动作难以驱动未见本体，像素空间锚定虽能改善零样本却未解决少样本灾难遗忘。
- Agent 执行层开始同时扩展真实手机等设备边界和跨桌面／CLI 的长时任务能力，持久可验证状态、独立审计、失败恢复与任务隔离正在成为 harness 的基础合同；具身侧的物理提示注入又表明，环境文字必须进入不可信输入与动作核验模型。

## 2026-08-03 新增判断

- 长期自治开始把电池、传感器、计算和内存老化纳入系统状态，用剩余硬件能力调节推理复杂度、规划时域与任务承诺；“自我健康模型”正从运维信息进入闭环决策。
- 世界模型的可靠性边界从平均预测误差扩展到潜空间尾部与全生命周期攻击面：异常潜状态、受污染的想象链和过度信任都可能制造规划风险或“预测性安全幻觉”。
- Agent 内存从单会话持久化走向团队级资产治理，对话记忆、Skill、Wiki 和代码图需要共享、装配、权限与来源管理；本地推理则继续沿低显存分层加载和原生跨 GPU 运行时两路发展。

## 2026-08-02 新增判断

- 具身数据引擎开始以空间标定和时间同步统一第一／第三视角、全身与手部运动、对象 6-DoF、音频和触觉；数据规模之外，跨模态闭环完整性成为新的基础设施指标。
- 世界模型的中间接口进一步显式化：离散“物理语言”把状态演化与视频渲染拆开，3D 动作位置编码则把末端几何注入第一视角生成，目标都指向可控、可审计的未来预测。
- 机器人地图同时向动态适应与轻量语义演进：锚点条件形变处理异质城市运动，对象级开放词汇索引则用一个数量级内存下降换取长期可查询性。
- GitHub Agent 栈继续拆分为协作工作台、确定性图编排和 Skill 录制层；超大 MoE 发布也迅速带动 NVMe 流式权重等存储感知推理工具。

## 2026-08-01 新增判断

- 具身策略的推理制度开始显式匹配物理阶段：接触前保留扩散多模态，接触后切换高频反应；“何时花计算”正成为与模型结构同等重要的设计变量。
- 触觉从辅助观测升级为世界模型监督接口，传感器侧的毫秒级三轴力感知与模型侧的力学未来预测正在合流，同时强调未来信息不能泄漏给部署动作分支。
- 多机器人协同从共享当前状态推进到通信受限的共同未来表征，去中心化 JEPA 已显示对未见群体规模和候选计划价值估计的迁移能力。
- GitHub Agent 工具链继续分层为桌面工作台、低资源 harness、研究 Skill 与官方嵌入 SDK；社区同时奖励可运行基础课程，反映“使用工具”和“形成能力”两端都存在强需求。

## 2026-07-31 新增判断

- 具身模型评测开始主动剥离底层控制误差，暴露“身体在哪里、是否到达、是否碰撞”的持续自我状态追踪才是长时行动瓶颈。
- 真机学习正在同时提高推理与数据效率：轻量 `V+L→A` 绕开逐周期 LLM，对称轨迹增强和自主练习则让每次物理交互产生更多可复用能力。
- Agent 工具链覆盖面继续扩展到真实浏览器验证、可安装研究流程和开放桌面执行环境，社区关注点从代码生成推进到完整工作的执行与核验。

## 2026-07-30 新增判断

- 驾驶自博弈开始从特权向量状态转向各智能体的第一视角图像，训练与部署观测一致性正成为与模拟吞吐同等重要的扩展维度。
- 具身学习同时重构数据、本体与时序：高保真 UMI 尝试取消真机后训练锚点，生成模型围绕目标运动协同设计机器人本体，快慢传感通道则让大策略适应真实控制延迟。
- GitHub 的 Agent 工程分化为轻量 harness 和混合审查控制面；社区一面追求本地低资源运行，一面用确定性规则约束 LLM 的生产决策。

## 2026-07-29 新增判断

- 自动驾驶优化目标开始从总效率扩展到分布公平：城市级车队协调需要显式审计不同行程长度与地区承担的延误，并在有限重规划预算内纠偏。
- 具身系统的“结构化接口”继续扩展：运动学中间表示连接人类视频与机器人策略，未来视觉监督连接触觉与 VLA，低成本机械臂农场则把真机评测转成可并行数据基础设施。
- 空间生产力工具在 GitHub 同步升温，3D 建筑编辑器与跨浏览器、桌面、移动端、Jupyter 的 GIS 工具表明空间数据正在走向 Web 化与协作化。

## 2026-07-28 新增判断

- 世界模型开始把机器人控制器、URDF 几何和触觉反馈纳入统一的动作条件接口，跨 embodiment 的可渲染中间层成为新方向。
- 4D 感知与柔顺控制都在显式编码结构先验：前者用 SE(3) 运动基底，后者用可旋转阻抗轴，物理结构重新成为泛化与复用支点。
- GitHub 社区对端侧 AI、Agent 记忆／环境、可运行教程和训练并行库的关注同步上升，表明热点已从模型展示扩展到可部署系统。

## 自动驾驶

### 感知与场景表征

近期路线从单任务检测转向统一空间表征与不确定性建模。BEV 仍是多摄像头融合主轴，但开始引入生成式候选分布、几何约束和语义协同；深度、占用和道路结构也更多作为规划可直接消费的中间表示，而非孤立 benchmark 输出。

### 规划、世界模型与 VLA

驾驶模型正把视觉—语言推理与轨迹预测绑得更紧：用轨迹一致性奖励约束 reasoning、用世界模型预测 ego dynamics、用 VLA 将场景理解连到控制。分层世界模型开始把长时粗场景结构与短时视觉细节拆开，并用 diffusion forcing 预训练表征、teacher forcing 稳定 rollout。快速／慢速双路径开始按场景复杂度分配推理预算，结构化 reasoning-action 一致性则由规则验证并反馈给强化学习。相比早期“描述场景再规划”，新工作更强调中间推理能否被几何轨迹、形式约束或闭环结果检验。

自博弈训练也开始处理观测接口本身：不再先用无遮挡精确状态训练教师再蒸馏给相机学生，而是直接为每个交通参与者渲染自车视角、在部分观测下扩展到数百亿步。后续判断重点是这种模拟视角一致性能否转化为真实相机闭环与长尾安全收益。

### 协同驾驶与多智能体

协同对象正在从低层状态、特征或传感器共享，提升到意图协商和全局决策。最新工作进一步用紧凑连续 proto-plan 广播全局策略，让各车以本地观测完成战术执行，显示“共享低维协调意图”可能成为多车扩展的新接口。车队层面的评价也开始超越平均旅行时间，转向审计不同行程长度与不同地区的延误分布，并以有预算的拥堵响应重规划兼顾效率与公平。边缘节点承担仲裁与审计，但通信延迟、错误意图、恶意参与者、中心协调失效和形式安全保证仍是从仿真走向真实道路的关键缺口。

### 安全与长尾

施工区、夜间、对抗扰动和稀有交互场景持续升温。研究方法从单纯扩数据集转向领域因果链、热成像感知、自动场景生成和可解释失效测试；下一步应重点看真实闭环、安全校准与跨域泛化。

## 机器人／具身智能

### VLA 与机器人基础模型

Scaling 不再只指参数量。新的扩展轴包括更长视觉—动作上下文、更多闭环 rollout、更高行为多样性、连续学习和小参数适配。推理期 fast weights、双时间尺度适配与冻结主干的小型接口层，反映出部署成本与持续学习正在成为核心指标。与此同时，VLA 工程开始把缓存正确性、数值稳定、控制频率对齐和异步流水线作为一等问题，闭环频率与反应延迟正在成为与任务成功率同等重要的指标。门店人形案例进一步显示，从实验室走向真实环境的主要增益可能来自数据筛选、视觉提示、价值诊断和系统集成，而非再换一个更大架构。

新的实时流策略把本体感知设为高频快通道、视觉语言特征设为异步慢通道，并按硬件延迟调整去噪调度。这表明“大骨干是否保留”与“控制是否实时”不必二选一，但视觉突发事件对慢通道刷新率的依赖仍需警惕。

### 操作与人形控制

操作研究强调接触、触觉、长时多阶段任务及人类纠正信号；数据策略也从全程端到端采集转向把学习预算集中在接触关键段，并以主动探测降低位姿不确定性。AXIS 一类系统又把浏览器采集、自动质检、增强、数据版本和留出评测连成可持续增长的数据引擎；低成本机械臂农场进一步把统一预算的真机评测并行化，并将成功、次优与失败轨迹重新沉淀为训练数据。语言抓取开始用 seed point 等轻接口解耦 VLM 的语义判断与轻量几何执行，关节物体操作则用运动学中间表示连接人类视频和机器人策略；双臂操作尝试把扩散先验与碰撞、运动学、交接等可微能量约束组合。人形控制同时沿大规模行为基础模型与低成本遥操作平台演进。数据治理、评分一致性、采样实时性、仿真到真机和安全恢复仍需持续跟踪。

高保真 UMI 正尝试跨过“只能预训练”的边界：通过毫米级轨迹、原生双手相对位姿、微秒同步与超宽视野，仅用无机器人示范完成后训练。与此同时，机器人本体也从固定前提变成生成变量，统一动力学模型可围绕目标运动联合优化结构与控制。

### 导航与空间推理

导航系统开始减少把 3D 场景“翻译成文本”的损失，转而直接向 VLM 注入连续 3D scene tokens；同时，新基准开始主动移除路线级语言中隐含的方向、距离与布局先验，只给目标视图并要求从局部视觉线索规划。当前未见环境成功率仍很低，说明长时证据整合、跨视角目标匹配和闭环稳定性尚未解决。

### 具身安全评测

安全评测正从静态危险识别和最终任务完成，扩展到动作过程中的空间关系、前置条件与连锁风险。新的 benchmark 开始揭示“任务做成但过程违规”的落差；后续关键是覆盖动态人类、接触力和长时风险，并防止语言模板捷径。

### 世界模型

世界模型与扩散策略的评价正在从视频逼真度转向决策吞吐、动作搜索预算和策略排序能力。单步或少步生成、action-centered 解码和训练期视觉动力学监督，都是为实时闭环降低成本；像素空间动作轨迹又开始作为跨 embodiment 接口，让同一视频模型兼做正向动力学与逆向动作生成。新的工程路线一面用块—时刻级缓存调度跳过冗余计算，一面允许长序列局部重新加噪、利用后续上下文修订早期决定。长期误差累积、多模态未来覆盖、额外采样延迟与动力学可行性仍是主要风险。

最新 WAM 进一步把监督从完整 RGB 外观重建转向夹爪、物体与接触区的动态变化，并用 token 级相关性路由推理预算。控制相关表征正在取代“更逼真的未来视频”成为主要目标。

## SLAM、3D 与长期自治

前馈 3D 重建、语义/几何融合 SLAM、多会话对象级变化检测和流式地图成为共同趋势。地图不再被视为静态几何，而是带对象身份、时间变化、语义对应与运动不确定性的长期记忆；连续具身问答进一步表明，仅保存 2D 可通行性不足，视觉语义证据需要锚定到度量 3D 几何。大尺度 3DGS SLAM 以稀疏锚点、流稠密化和空间分区压住长序列的实时与显存成本；结构光路线则显示，若前端深度足够稠密且度量准确，深度主导、仅在退化处引入稀疏视觉和轻量 BA 的简单系统也能稳健实时。另一条互补路线把经典物理模型直接写入学习目标。动态物体、跨季节变化、户外传感边界、回环一致性、置信度校准及真实算力仍是关键判断维度。

## GitHub 开源趋势

### Agent Skills 与工作流资产化

社区正在把 prompt 升级为可版本化、可安装、可组合的 skills。设计、工程实践和内容蒸馏等技能库增长快，说明用户希望复用稳定流程，而不是每次从零描述任务。新一批媒体 Skill 进一步打包镜头配方、运动预览和可执行模板，开始直接交付领域成品；技术书转 Skill 和视频观看工作流又表明，长文档知识与复杂多媒体输入都在被封装为可复用能力。

### Agent 开发环境与上下文基础设施

并行 Agent 环境、MCP 桌面工具、代码知识图谱、本地优先检索和跨工具记忆层持续热门。瓶颈正在从“模型会不会写代码”转向 harness 设计、任务隔离、上下文选择、可观察性、成本和隐私边界；worker、上下文压缩、模型路由、原生多线程操作台与 TUI 目录正在形成独立工具层。

新的分化一端是以 Rust 实现低内存常驻 harness，另一端是将确定性规则流水线与 LLM Agent 结合的代码审查系统。资源效率与决策可审计性正在成为生产 Agent 的两项独立竞争指标。

### Agent 执行治理与供应链

随着 Agent 获得浏览器、终端和业务系统权限，治理正在从被动日志转向贯穿执行生命周期的控制面：动作前授权、执行中观测、执行后验证。同时，模型、harness、数据集、工具与最终产物需要稳定身份和 provenance，方便调试、复现、合规与责任追踪。后续应重点看策略表达能力、绕过风险、性能开销和跨平台互操作性。

### Agent 进入真实生产力场景

Office 文档自动化、产品分析、日志与 session replay、视频编辑等终端产品获得显著关注。社区更奖励能直接解决完整工作流的开源应用，而不只是模型 wrapper 或概念 demo。

### 机器人与空间智能开源

流式前馈 3D 重建进入 Trending，说明空间智能开始从论文热点外溢到通用开源社区。3D 建筑编辑器与跨 Web、桌面、移动端、Jupyter 的 GIS 工具进一步显示，空间智能正在形成面向创作、分析与分享的通用生产力层。后续值得观察其是否形成稳定 SDK、数据接口和机器人实时部署案例。
## 2026-08-13 新增判断

- 驾驶／VLA 的动态计算开始同时作用于语义推理和底层缓存：可执行短 token 缩减推理链，动作置信度则决定视觉 KV 何时必须重算，实时性优化正转向风险感知的预算分配。
- 世界模型评价继续脱离像素逼真度：无动作视频要用闭环成功率证明迁移价值，latent 指标也要覆盖 encoder、predictor 与 planner，能否支持规划成为共同尺度。
- 长期自治逐渐形成证据—记忆—风险三层状态：导航显式累积正负证据，4D 场景图维护对象变化，CVaR 规划约束尾部失败。
- GitHub 社区同时追捧可编辑视觉／Office 成品、统一 Agent 工作区与端侧小模型，生产力的下一轮竞争集中在交付质量、上下文整合和本地控制权。

## 2026-08-14 新增判断

- 自动驾驶安全链条开始把稀疏人工停止反馈、确定性规划监督、风险切片覆盖和渐进攻击检测串起来；安全保证正从单一模型指标转向训练、执行与审计的分层证据。
- 机器人策略的条件化失败成为独立研究对象：初始姿态可诱导选手偏置，结构化单示范可在测试时修复 OOD，而接触求解器的收益也随任务结构变化，平均成功率不足以描述部署边界。
- GitHub 的 Agent 栈继续上下分层：可编辑视觉成品与角色契约位于应用层，模型路由、图原生上下文、跨工具记忆和端侧小模型形成基础层；可管理的上下文与资源成本正取代单纯工具数量成为竞争点。
